# This explicit development image gives the MTU3 gadget to USB Ethernet.
# Stock MTP must not reconfigure the same controller or unload g_ether.
description "MTP excluded by the developer USB Ethernet profile"
start on qemu-disabled-service
task
exec true
