# This optional service has no backend in the QEMU machine.
description "optional service disabled by the QEMU device model"

# This event is deliberately never emitted.  Keeping a valid inert job makes
# Upstart report stop/waiting instead of repeatedly respawning a missing
# payload or rejecting an unsupported 'manual' stanza.
start on qemu-disabled-service

task
exec true
