# The 5.12.2.2 recovery rootfs contains this Upstart job but not the perfd
# executable. Leaving the stock respawn stanza active creates an unbounded
# start/exit loop, so keep the unavailable optional daemon disabled.
manual
