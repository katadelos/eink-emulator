# Starting events block the consumer until this task finishes. Language picker
# starts after the data filesystems settle, avoiding transient var-local mounts.
description "Initialize offline Kindle Home before the first UI startup"
start on starting langpicker or starting lab126_gui
stop on stopping filesystems
task
exec /usr/sbin/qemu-offline-setup
